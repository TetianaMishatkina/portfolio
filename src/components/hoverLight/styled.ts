import styled from "styled-components/macro";

export const StyledHoverLight = styled.div`
  width: 100%;
  height: 100vh;
  position: absolute;
  z-index: 0;
`;

export const StyledLight = styled.div`
  inset: 0;
  z-index: 999;
  position: absolute;
  pointer-events: none;
`;
